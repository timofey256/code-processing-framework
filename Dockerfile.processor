FROM debian:stable-slim AS build

RUN apt-get update && apt-get install -y --no-install-recommends \
    g++ cmake make pkg-config git \
    libasio-dev libssl-dev nlohmann-json3-dev librabbitmq-dev curl libboost-all-dev \
    docker-cli libhiredis-dev libpq-dev libpqxx-dev \
 && rm -rf /var/lib/apt/lists/*

WORKDIR /app
COPY . .

RUN rm -rf build && mkdir build && cd build && \
    cmake .. -DCMAKE_BUILD_TYPE=Release && \
    make -j$(nproc) processor

FROM debian:stable-slim AS runtime

RUN apt-get update && apt-get install -y --no-install-recommends \
    libssl3 librabbitmq4 curl docker-cli libhiredis-dev libpq5 libpqxx-dev \
 && rm -rf /var/lib/apt/lists/*

WORKDIR /app
COPY --from=build /app/build/processor /usr/local/bin/processor

VOLUME ["/var/run/docker.sock"]

CMD ["processor"]
